<?xml version="1.0" encoding="utf-8"?>
<!--
    initialLayout이 없으면 AppWidgetHostView.getDefaultView()가
    Resources$NotFoundException(Resource ID #0x0)으로 실패하고 시스템 에러 뷰
    ("위젯을 추가할 수 없습니다")로 폴백한다. Glance가 RemoteViews를 밀어넣기
    전까지 그 상태가 유지되고, 앱이 stopped 상태면 영원히 남는다.
    Glance가 AAR로 제공하는 로딩 레이아웃을 지정해 빈 구간을 메운다.

    previewLayout은 위젯 선택기 미리보기다. 없으면 흰 빈 사각형만 보인다.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:targetCellWidth="3"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:previewLayout="@layout/glance_default_loading_layout"
    android:description="@string/widget_description" />
